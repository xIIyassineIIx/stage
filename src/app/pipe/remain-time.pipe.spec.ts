import { RemainTimePipe } from './remain-time.pipe';

describe('RemainTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RemainTimePipe();
    expect(pipe).toBeTruthy();
  });
});
